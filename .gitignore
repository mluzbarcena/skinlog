node_modules
dist
*.local
# Local Firebase config (never commit real values); keep the template.
.env
.env.*
!.env.example
.DS_Store
skinlog-dist.zip
*.tsbuildinfo
BACKLOG.md

# Private, machine-local AI config (never pushed). *.local above does not
# match these because of the .json/.md suffixes.
.claude/settings.local.json
AGENTS.local.md
